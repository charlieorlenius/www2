{% if resume.work.size > 0 %}

%-------------------------------------------------------------------------------
%	SECTION TITLE
%-------------------------------------------------------------------------------
\cvsection{ {{ strings.resume.work | default:"Work experience" }} }


%-------------------------------------------------------------------------------
%	CONTENT
%-------------------------------------------------------------------------------
\begin{cventries}

{% for work in resume.work %}
  {% if work.endDate.size > 0 %}
    {% assign endDate = work.endDate | date:from_to_format %}
  {% else %}
    {% assign endDate = 'present' %}
  {% endif %}

%---------------------------------------------------------
  \cventry
    { {{ work.position | replace: "&", "\\&" }} } % Job title
    { {{ work.company | replace: "&", "\\&" }} } % Organization
    { {{ work.location.city | replace: "&", "\\&" }} } % Location
    { {{ work.startDate | date:from_to_format }}-{{endDate}} } % Date(s)
    {
      {{ work.summary | replace: "&", "\\&" }}
      \vspace{\baselineskip}
      \begin{cvitems}{% for highlight in work.highlights %}
        \item{ {{ highlight | replace: "&", "\\&" }} }{% endfor %}
      \end{cvitems}
      }
  
{% endfor %}


%---------------------------------------------------------
\end{cventries}

{% endif %}
