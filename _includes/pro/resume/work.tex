{% if resume.work.size > 0 %}

%-------------------------------------------------------------------------------
%	SECTION TITLE
%-------------------------------------------------------------------------------
\cvsection{Work Experience}


%-------------------------------------------------------------------------------
%	CONTENT
%-------------------------------------------------------------------------------
\begin{cventries}

{% for work in resume.work %}
  {% if work.endDate.size > 0 %}
    {% assign endDate = work.endDate | date:from_to_format %}
  {% else %}
    {% assign endDate = 'present' %}
  {% endif %}

%---------------------------------------------------------
  \cventry
    { {{ work.position }} } % Job title
    { {{ work.company }} } % Organization
    {Seoul, S.Korea} % Location
    { {{ work.startDate | date:from_to_format }}-{{endDate}} } % Date(s)
    {
      {{ work.summary }}
      \vspace{\baselineskip}
      \begin{cvitems}{% for highlight in work.highlights %}
        \item{ {{ highlight }} }{% endfor %}
      \end{cvitems}
      }
  
{% endfor %}


%---------------------------------------------------------
\end{cventries}

{% endif %}
