{% if resume.skills.size > 0 %}
%-------------------------------------------------------------------------------
%	SECTION TITLE
%-------------------------------------------------------------------------------
\cvsection{ {{ strings.resume.skills | default:"Skills" }} }

{% for skl in resume.skills %}

%-------------------------------------------------------------------------------
%	SUBSECTION TITLE
%-------------------------------------------------------------------------------
\cvsubsection{ {{ skl.name | replace: "&", "\\&" }} }{% if skl.keywords.size > 0 %}
\headersocialstyle{ 
\newline{% for key in skl.keywords %}
{ {{ key | replace: "&", "\\&" }} } {% if forloop.last == false %}\acvHeaderSocialSep{% endif %}{% endfor %}
}
{% endif %}

{% endfor %}

{% endif %}
